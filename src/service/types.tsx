export type Nft = {
  tokenId: string;
  tokenUri: string;
  name: string;
  descriptipn: string;
  imageUri: string;
  uri: string;
  type: string;
};
export type NftMeta = {
  name: string;
  descriptipn: string;
  imageUri: string;
  uri: string;
  type: string;
};
